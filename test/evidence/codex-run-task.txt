--- $ node /home/user/Projects/moat/cmd/main.ts run Make the failing test pass, then commit it.
--- exit 0
--- stdout ---
--- stderr ---
sandbox already running (pid 389110); restarting it for this run
profiles: everything requested is already installed
copy-in: reusing the sandbox working tree (use --sync to re-copy from the host)
model: moat/mock-model
! injecting moat credential sha256:34c4e933b47c1fb3 (ttl 14400s) as MOAT_INJECTED_CREDENTIAL. The agent can read this value and, with the network open, exfiltrate it. Use a spend-capped key with a low limit. See docs/SPEC.md §1.2.
agent branch: moat-session-2026-09-21-01-34
checks: npm run test
→ sandbox booted (pid 389612); the codex runtime has no server to wait for
  ✓ /bin/sh -lc "cd /work && sed -i 's/a - b/a + b/' src/sum.js && npm test 2>&1 | tail -4 && git add -A && git -c user.emai

Fixed src/sum.js so the test passes, and committed it.
  17280 tokens  1 tool
