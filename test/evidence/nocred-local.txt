--- $ node /home/user/Projects/moat/cmd/main.ts run --no-credential --no-detect --model mock-model --base-url http://127.0.0.1:43295/v1 do the task
--- exit 0
--- stdout ---
--- stderr ---
egress: open, because the provider is on the host's loopback (http://127.0.0.1:43295/v1) and the sandbox's own namespace cannot reach it. Pass --egress filtered to filter it anyway.
→ provisioning sandbox image (alpine 3.21.4 + codex 0.155.1)
→ provisioning from the cached image alpine-3.21.4-5fcd2d224a5c.tar.gz
✓ image provisioned in 1.46s (extract cached image 1.26s)
→ copy-in: git clone --no-hardlinks
✓ copy-in via git: 1 files, 0.0 KiB, digest 3b51b9bbd854d89f
model: moat/mock-model
! no credential injected, so requests to http://127.0.0.1:43295/v1 will carry no Authorization header. If that endpoint needs one, set DEEPSEEK_API_KEY or pass --credential-env NAME (or --credential VALUE).
agent branch: moat-session-2026-09-21-08-39
→ sandbox booted (pid 460120); the codex runtime has no server to wait for
  ✗ /bin/sh -lc "cd /work && sed -i 's/a - b/a + b/' src/sum.js && git add -A && git -c user.email=mock@example.com -c user.

Fixed src/sum.js so the test passes, and committed it.
  17280 tokens  1 tool · 1 failed
