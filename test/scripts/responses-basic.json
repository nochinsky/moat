{
  "model": "mock-model",
  "turns": [
    { "tool": { "cmd": "cd /work && sed -i 's/a - b/a + b/' src/sum.js && git add -A && git -c user.email=mock@example.com -c user.name=mock commit -qm 'fix: sum adds' && git log --oneline -1" } },
    { "text": "Fixed src/sum.js so the test passes, and committed it." }
  ]
}
